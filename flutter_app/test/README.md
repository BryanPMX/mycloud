# `flutter_app/test`

Client unit and widget tests live here.

Group tests by core, features, and shared UI concerns for easy discovery.

Current coverage:
- `test/core/smoke_test.dart` boots the app, signs in through the current demo auth shell, and verifies route navigation into the albums view
