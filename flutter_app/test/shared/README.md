# `flutter_app/test/shared`

Shared widget and utility tests live here.

Use this directory for app-wide reusable components and helper coverage.

Current helper:
- `demo_app_harness.dart` centralizes the seeded demo `AppConfig`, app bootstrapping, and section/sign-in helpers so widget and integration coverage stay in sync.
