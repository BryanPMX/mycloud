# `flutter_app/test/features/directory`

Use this directory for family-directory and avatar-cache coverage, especially the contract between `GET /users/directory`, `GET /users/:id/avatar`, and the shared `user_id`-keyed cache.
