# `flutter_app/lib/features/directory/providers`

Use this directory for family-directory loading, avatar URL refresh logic, and other user-directory state that can be shared across profile, albums, comments, and shell UI.
