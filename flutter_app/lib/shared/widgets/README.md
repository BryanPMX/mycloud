# `flutter_app/lib/shared/widgets`

Reusable client widgets live here.

Keep feature-agnostic building blocks in this directory.

Current shared slice on March 15, 2026:
- `main_scaffold.dart` owns the adaptive authenticated shell
- `user_avatar.dart` now renders initials plus cached signed avatar URLs that refresh through the shared family-directory provider
