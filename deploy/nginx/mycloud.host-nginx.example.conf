server {
    listen 80;
    server_name api.mynube.live;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name api.mynube.live;

    ssl_certificate /etc/letsencrypt/live/api.mynube.live/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.mynube.live/privkey.pem;

    client_max_body_size 100m;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name minio.mynube.live;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name minio.mynube.live;

    ssl_certificate /etc/letsencrypt/live/minio.mynube.live/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/minio.mynube.live/privkey.pem;

    client_max_body_size 5g;
    proxy_buffering off;
    proxy_request_buffering off;

    location / {
        proxy_pass http://127.0.0.1:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;
    server_name console.mynube.live;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name console.mynube.live;

    ssl_certificate /etc/letsencrypt/live/console.mynube.live/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/console.mynube.live/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:9001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
