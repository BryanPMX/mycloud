# `internal/infrastructure/observability`

Shared backend logging and metrics helpers live here.

This directory should centralize instrumentation concerns that are reused across delivery and worker code.
