# `internal`

This directory contains backend application code that should remain private to this module.

The main subsystems are:
- `domain/` for business contracts
- `application/` for use cases
- `infrastructure/` for external adapters
- `delivery/` for transport-facing code
