server {
    listen 80;
    server_name _;

    # Placeholder vhost. Replace with TLS, SPA, API, WebSocket, and MinIO routes later.
    location / {
        return 200 "MyCloud nginx placeholder\n";
    }
}
